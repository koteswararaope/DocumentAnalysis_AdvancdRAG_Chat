{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug FastAPI",
            "type": "debugpy",
            "request": "launch",
            "module": "uvicorn",
            "args": [
                "api.main:app",          // change if your app is in api/main.py → "api.main:app"
                "--port", "8000"
            ],
            "jinja": true,
            "justMyCode": true
        }
    ]
}